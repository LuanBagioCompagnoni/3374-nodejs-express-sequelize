const path = require('path');


/* 

    Por conta da estrutura do nosso projeto, as pastas geradas pelo sequelize (config, migrations, seeders e models) foram realocadas para a pasta /src.

    Este arquivo serve para mostrar ao sequelize-cli onde estão localizadas as pastas que foram criadas.

*/
module.exports = {
    'config': path.resolve('./src/config', 'config.json'),
    'models-path': path.resolve('./src/models'),
    'seeders-path': path.resolve('./src/seeders'),
    'migrations-path': path.resolve('./src/migrations'),
}